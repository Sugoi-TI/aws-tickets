{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "bin/**", "lib/**"]
    },
    "@my-app/shared#build": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  },
  "globalEnv": ["VITE_*"]
}